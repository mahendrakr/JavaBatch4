package functionalInterface;

public class FunInterCon {
	/**
	 * 
	 * functional interface have only one abstract method. but many number of
	  default and static method can be present in functional interface.
	  
	  
	 * DEFAULT FUNCTIONAL INTERFACE METHODS->
	                         runable = run();
	                         callable = call();
	                        camprable = campareTO();
                        actionListene= actionperpormed(); 
	 *  (NOTE:-Lambdaexpression only can invoke functional interface.)
             
             
         *Predefined function interfaces:->
                                      # Predicate interface
                                      # Function interface
                                      # Consumer interface
                                      # Supplier interface    
	 */
     
}
